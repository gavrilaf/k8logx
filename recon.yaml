---
pods:
  - api:
    pattern: "api-deployment"
    containers:
      - api:
        pattern: "recon-api-app"
        fields-order:
          - ["method", "uri", "status", "latency"]
          - ["sql"]
          - ["args", "rowCount"]
        show-all: true
  - dispatcher:
    pattern: "dispatcher"